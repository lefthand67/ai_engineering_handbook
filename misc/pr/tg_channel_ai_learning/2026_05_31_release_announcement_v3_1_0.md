https://soviar-systems.github.io/ai_engineering_handbook/
**Наконец, release v3.1.0 — Прощай, «интуитивный» инжинеринг: переходим к верифицируемым ограничениям**

💡 Главная проблема разработки с AI — «диагностический цикл». Агент ошибается -> пользователь исправляет -> агент извиняется, но повторяет ошибку, потому что инструкции разбросаны по файлам, а критерии качества определяются «вайбами» (то есть полностью полагаются на способности ИИ-модели).

🧠 Трансформеры страдают от «авторегрессионного импульса»: из-за природы предсказания наиболее вероятного токена LLM часто цепляется за первую правдоподобную версию ответа и просто соглашается с самой собой, игнорируя альтернативы. Без принудительного поиска опровержений агент превращается в эхо-камеру собственных галлюцинаций.

⚙️ В v3.1.0 мы переходим к концепции **Prompt-as-Infrastructure**. Теперь промпты — это не просто статичные тексты, а собираемые конструкции: мы описываем логику в отдельных JSON-блоках, объединяем их в манифесте и компилируем в финальный YAML-промпт. Это радикально снижает соотношение шума к сигналу и экономит место в контекстном окне.

✅ **Код как первичная документация**: Переходим на Contract Docstrings. Теперь инструкции — это часть самого исходного кода, а их достоверность подтверждается обязательным набором тестов. Это решило проблему «дрейфа данных» (data drift) и накопившегося техдолга в документации: вместо громоздкой триады (код+тест+док) мы перешли к **диаде «скрипт-тест»**.

✅ **Метрика WRC**: Хватит верить «уверенному тону» нейросети. WRC (Weighted Response Confidence) — это механизм внутренней дисциплины модели, работающий как «мягкий» ограничитель (soft guardrail). Модель всё ещё может галлюцинировать, но WRC заставляет её обосновывать уверенность конкретными доказательствами и соблюдением ограничений, а не лингвистической беглостью.

✅ **Скрипты-инструкторы**: Валидаторы больше не пишут «Error: invalid format». Они дают Just-in-Time-инструкции: что именно сломано, как это исправить за один шаг и где прочитать правила, избавляя от бесконечных циклов поиска решения ошибок.

[Разбор стратегического сдвига в RELEASE_NOTES.md](https://github.com/soviar-systems/ai_engineering_handbook/releases/tag/v3.1.0)

#Python #LLM #AI_Engineering #MLOps #AgenticWorkflow #ai_engineering_handbook
